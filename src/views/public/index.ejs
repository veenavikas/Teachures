<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teachures — Learn Without Limits</title>
    <meta name="description"
        content="World-class learning platform for creators and learners. Enroll in 400+ courses, earn verified certificates, and build your future.">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Sans:ital,wght@0,400;0,500;0,700;1,400&family=Inter:wght@300;400;500;600&display=swap"
        rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        *,
        *::before,
        *::after {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        :root {
            --yellow: #F5C518;
            --yellow-dim: #D4AA00;
            --black: #050505;
            --surface: #0E0E0E;
            --border: rgba(255, 255, 255, 0.06);
            --text: rgba(255, 255, 255, 0.85);
            --muted: rgba(255, 255, 255, 0.4);
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            background: var(--black);
            color: var(--text);
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* NOISE OVERLAY */
        body::before {
            content: '';
            position: fixed;
            inset: 0;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='1'/%3E%3C/svg%3E");
            opacity: 0.015;
            pointer-events: none;
            z-index: 0;
        }

        /* NAV */
        nav {
            position: sticky;
            top: 0;
            z-index: 100;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 1.2rem 5%;
            background: rgba(5, 5, 5, 0.85);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border);
        }

        .nav-logo {
            font-family: 'Bebas Neue', sans-serif;
            font-size: 1.7rem;
            letter-spacing: 3px;
            color: #fff;
            text-decoration: none;
        }

        .nav-logo span {
            color: var(--yellow);
        }

        .nav-links {
            display: flex;
            gap: 2.5rem;
        }

        .nav-links a {
            color: var(--muted);
            font-size: 0.9rem;
            font-weight: 500;
            text-decoration: none;
            transition: color 0.2s;
        }

        .nav-links a:hover {
            color: #fff;
        }

        .nav-actions {
            display: flex;
            gap: 1rem;
            align-items: center;
        }

        .btn-nav-outline {
            padding: 0.5rem 1.25rem;
            border: 1px solid var(--border);
            border-radius: 8px;
            color: var(--muted);
            font-size: 0.85rem;
            font-weight: 500;
            text-decoration: none;
            transition: all 0.2s;
        }

        .btn-nav-outline:hover {
            border-color: rgba(255, 255, 255, 0.2);
            color: #fff;
        }

        .btn-nav-primary {
            padding: 0.5rem 1.25rem;
            background: var(--yellow);
            border-radius: 8px;
            color: #000;
            font-size: 0.85rem;
            font-weight: 700;
            text-decoration: none;
            transition: all 0.2s;
        }

        .btn-nav-primary:hover {
            background: var(--yellow-dim);
            box-shadow: 0 4px 15px rgba(245, 197, 24, 0.3);
        }

        /* HERO */
        .hero {
            min-height: 92vh;
            display: flex;
            align-items: center;
            padding: 5rem 5%;
            position: relative;
            overflow: hidden;
        }

        .hero-glow {
            position: absolute;
            width: 800px;
            height: 800px;
            background: radial-gradient(circle, rgba(245, 197, 24, 0.08) 0%, transparent 65%);
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            pointer-events: none;
        }

        .hero-grid {
            display: grid;
            grid-template-columns: 1.1fr 0.9fr;
            gap: 4rem;
            align-items: center;
            max-width: 1280px;
            margin: 0 auto;
            width: 100%;
            position: relative;
            z-index: 1;
        }

        .hero-eyebrow {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: rgba(245, 197, 24, 0.1);
            border: 1px solid rgba(245, 197, 24, 0.2);
            border-radius: 50px;
            padding: 5px 14px 5px 8px;
            margin-bottom: 1.75rem;
        }

        .eyebrow-dot {
            width: 6px;
            height: 6px;
            background: var(--yellow);
            border-radius: 50%;
        }

        .eyebrow-text {
            font-size: 0.78rem;
            color: var(--yellow);
            font-weight: 600;
            letter-spacing: 1.5px;
            text-transform: uppercase;
        }

        .hero-title {
            font-family: 'Bebas Neue', sans-serif;
            font-size: clamp(4.5rem, 10vw, 8.5rem);
            line-height: 0.9;
            letter-spacing: 2px;
            color: #fff;
            margin-bottom: 1.75rem;
        }

        .hero-title .accent {
            background: linear-gradient(135deg, #F5C518 20%, #FFD700 100%);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-subtitle {
            font-size: 1.15rem;
            color: var(--muted);
            line-height: 1.75;
            max-width: 480px;
            margin-bottom: 2.5rem;
        }

        .hero-ctas {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
            margin-bottom: 3rem;
        }

        .btn-primary {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 0.9rem 2rem;
            background: var(--yellow);
            color: #000;
            border-radius: 10px;
            font-weight: 700;
            font-size: 0.95rem;
            letter-spacing: 0.5px;
            text-decoration: none;
            transition: all 0.3s;
        }

        .btn-primary:hover {
            background: var(--yellow-dim);
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(245, 197, 24, 0.35);
        }

        .btn-ghost {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 0.9rem 2rem;
            background: rgba(255, 255, 255, 0.04);
            color: #fff;
            border: 1px solid var(--border);
            border-radius: 10px;
            font-weight: 600;
            font-size: 0.95rem;
            text-decoration: none;
            transition: all 0.3s;
        }

        .btn-ghost:hover {
            background: rgba(255, 255, 255, 0.08);
            border-color: rgba(255, 255, 255, 0.15);
        }

        .hero-social-proof {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .avatars {
            display: flex;
        }

        .avatars span {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            border: 2px solid var(--black);
            background: linear-gradient(135deg, #333, #111);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.9rem;
            margin-left: -10px;
        }

        .avatars span:first-child {
            margin-left: 0;
        }

        .proof-text {
            font-size: 0.85rem;
            color: var(--muted);
        }

        .proof-text strong {
            color: #fff;
        }

        /* HERO VISUAL */
        .hero-visual {
            position: relative;
            display: flex;
            justify-content: center;
        }

        .hero-card {
            background: rgba(14, 14, 14, 0.9);
            border: 1px solid var(--border);
            border-radius: 20px;
            overflow: hidden;
            width: 100%;
            max-width: 420px;
            box-shadow: 0 40px 80px rgba(0, 0, 0, 0.7), 0 0 0 1px rgba(255, 255, 255, 0.04);
            animation: floatCard 5s ease-in-out infinite;
        }

        @keyframes floatCard {

            0%,
            100% {
                transform: translateY(0);
            }

            50% {
                transform: translateY(-14px);
            }
        }

        .card-thumb {
            height: 200px;
            background: linear-gradient(135deg, #1a1a2e, #0f0f1a);
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .card-thumb::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(to bottom, transparent 40%, rgba(14, 14, 14, 0.95));
        }

        .card-thumb-icon {
            width: 64px;
            height: 64px;
            color: rgba(245, 197, 24, 0.25);
        }

        .card-body {
            padding: 1.5rem;
        }

        .card-cat {
            font-size: 0.7rem;
            color: var(--yellow);
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 0.5rem;
        }

        .card-title {
            font-weight: 700;
            color: #fff;
            font-size: 1.05rem;
            line-height: 1.4;
            margin-bottom: 1rem;
        }

        .card-progress-label {
            display: flex;
            justify-content: space-between;
            font-size: 0.78rem;
            color: var(--muted);
            margin-bottom: 0.5rem;
        }

        .progress-track {
            height: 5px;
            background: rgba(255, 255, 255, 0.08);
            border-radius: 3px;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            width: 68%;
            background: linear-gradient(90deg, var(--yellow), #FFD700);
            box-shadow: 0 0 10px rgba(245, 197, 24, 0.5);
            border-radius: 3px;
        }

        .card-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 1.25rem;
            padding-top: 1.25rem;
            border-top: 1px solid var(--border);
        }

        .card-rating {
            color: var(--yellow);
            font-size: 0.85rem;
            font-weight: 700;
        }

        .card-students {
            font-size: 0.8rem;
            color: var(--muted);
        }

        /* Floating badges */
        .badge-float {
            position: absolute;
            background: rgba(14, 14, 14, 0.95);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 0.75rem 1rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(10px);
        }

        .badge-float-1 {
            top: -20px;
            right: -20px;
        }

        .badge-float-2 {
            bottom: 40px;
            left: -30px;
        }

        .badge-icon {
            width: 36px;
            height: 36px;
            background: rgba(245, 197, 24, 0.1);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .badge-icon svg {
            width: 20px;
            height: 20px;
            color: var(--yellow);
        }

        .badge-label {
            font-size: 0.7rem;
            color: var(--muted);
        }

        .badge-value {
            font-weight: 700;
            color: #fff;
            font-size: 0.9rem;
        }

        /* STATS */
        .stats-bar {
            border-top: 1px solid var(--border);
            border-bottom: 1px solid var(--border);
            padding: 3.5rem 5%;
        }

        .stats-inner {
            display: flex;
            justify-content: space-around;
            max-width: 900px;
            margin: 0 auto;
        }

        .stat-n {
            font-family: 'Bebas Neue', sans-serif;
            font-size: 4rem;
            line-height: 1;
            background: linear-gradient(135deg, var(--yellow), #FFD700);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .stat-l {
            font-size: 0.78rem;
            color: var(--muted);
            text-transform: uppercase;
            letter-spacing: 2.5px;
            font-weight: 700;
            margin-top: 4px;
        }

        /* HOW IT WORKS */
        .section {
            padding: 7rem 5%;
        }

        .section-label {
            font-size: 0.8rem;
            font-weight: 700;
            color: var(--yellow);
            letter-spacing: 3px;
            text-transform: uppercase;
            text-align: center;
            margin-bottom: 0.75rem;
        }

        .section-title {
            font-family: 'Bebas Neue', sans-serif;
            font-size: clamp(2.5rem, 5vw, 4rem);
            letter-spacing: 2px;
            text-align: center;
            color: #fff;
            margin-bottom: 4rem;
        }

        .steps-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 1.5rem;
            max-width: 1100px;
            margin: 0 auto;
        }

        .step {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 18px;
            padding: 2.5rem 2rem;
            transition: all 0.35s;
            position: relative;
        }

        .step::before {
            content: '';
            position: absolute;
            inset: 0;
            border-radius: 18px;
            background: linear-gradient(135deg, rgba(245, 197, 24, 0.04), transparent);
            opacity: 0;
            transition: opacity 0.3s;
        }

        .step:hover {
            border-color: rgba(245, 197, 24, 0.2);
            transform: translateY(-6px);
        }

        .step:hover::before {
            opacity: 1;
        }

        .step-num {
            font-family: 'Bebas Neue', sans-serif;
            font-size: 5rem;
            line-height: 1;
            color: rgba(245, 197, 24, 0.08);
            margin-bottom: 1rem;
        }

        .step-icon-wrap {
            width: 52px;
            height: 52px;
            background: rgba(245, 197, 24, 0.1);
            border-radius: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1.25rem;
        }

        .step-icon-wrap svg {
            width: 26px;
            height: 26px;
            color: var(--yellow);
        }

        .step h3 {
            font-size: 1.1rem;
            font-weight: 700;
            color: #fff;
            margin-bottom: 0.75rem;
        }

        .step p {
            color: var(--muted);
            font-size: 0.9rem;
            line-height: 1.7;
        }

        /* FEATURES */
        .features-section {
            padding: 7rem 5%;
            background: linear-gradient(180deg, transparent, rgba(245, 197, 24, 0.02), transparent);
        }

        .features-inner {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 5rem;
            align-items: center;
            max-width: 1100px;
            margin: 0 auto;
        }

        .features-list {
            display: flex;
            flex-direction: column;
            gap: 1.75rem;
        }

        .feature-item {
            display: flex;
            gap: 1.25rem;
        }

        .feature-icon-wrap {
            width: 44px;
            height: 44px;
            background: rgba(245, 197, 24, 0.08);
            border: 1px solid rgba(245, 197, 24, 0.15);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
        }

        .feature-icon-wrap svg {
            width: 22px;
            height: 22px;
            color: var(--yellow);
        }

        .feature-title {
            font-weight: 700;
            color: #fff;
            margin-bottom: 0.3rem;
            font-size: 0.98rem;
        }

        .feature-desc {
            color: var(--muted);
            font-size: 0.88rem;
            line-height: 1.6;
        }

        .feature-showcase {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 20px;
            padding: 2.5rem;
        }

        /* CTA BANNER */
        .cta-banner {
            margin: 0 5% 7rem;
            padding: 4rem 4rem;
            background: linear-gradient(135deg, rgba(245, 197, 24, 0.08), rgba(245, 197, 24, 0.03));
            border: 1px solid rgba(245, 197, 24, 0.12);
            border-radius: 24px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 2rem;
            flex-wrap: wrap;
        }

        .cta-banner h2 {
            font-family: 'DM Sans', sans-serif;
            font-size: 2rem;
            font-weight: 700;
            color: #fff;
            max-width: 500px;
            line-height: 1.3;
        }

        .cta-banner p {
            color: var(--muted);
            margin-top: 0.5rem;
            font-size: 0.95rem;
        }

        /* FOOTER */
        footer {
            border-top: 1px solid var(--border);
            padding: 4rem 5%;
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 3rem;
        }

        .footer-brand-name {
            font-family: 'Bebas Neue', sans-serif;
            font-size: 1.8rem;
            letter-spacing: 2px;
            color: #fff;
            margin-bottom: 0.75rem;
        }

        .footer-brand-name span {
            color: var(--yellow);
        }

        .footer-tagline {
            color: var(--muted);
            font-size: 0.88rem;
            line-height: 1.7;
            max-width: 280px;
        }

        .footer-col h4 {
            font-size: 0.75rem;
            color: rgba(255, 255, 255, 0.3);
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 1.25rem;
            font-weight: 700;
        }

        .footer-col a {
            display: block;
            color: var(--muted);
            font-size: 0.88rem;
            text-decoration: none;
            margin-bottom: 0.75rem;
            transition: color 0.2s;
        }

        .footer-col a:hover {
            color: #fff;
        }

        .footer-bottom {
            border-top: 1px solid var(--border);
            padding: 1.5rem 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.8rem;
            color: rgba(255, 255, 255, 0.2);
        }

        @media (max-width: 900px) {
            .hero-grid {
                grid-template-columns: 1fr;
                text-align: center;
            }

            .hero-ctas {
                justify-content: center;
            }

            .hero-social-proof {
                justify-content: center;
            }

            .hero-visual {
                display: none;
            }

            .features-inner {
                grid-template-columns: 1fr;
            }

            footer {
                grid-template-columns: 1fr 1fr;
            }

            .cta-banner {
                flex-direction: column;
                text-align: center;
            }
        }

        @media (max-width: 600px) {
            nav .nav-links {
                display: none;
            }

            footer {
                grid-template-columns: 1fr;
            }

            .stats-inner {
                flex-wrap: wrap;
                gap: 2rem;
            }
        }
    </style>
</head>

<body>

    <!-- NAV -->
    <nav>
        <a href="/" class="nav-logo">TEACH<span>URES</span></a>
        <div class="nav-links">
            <a href="/courses">Courses</a>
            <a href="/pricing">Pricing</a>
            <a href="/about">About</a>
        </div>
        <div class="nav-actions">
            <a href="/login" class="btn-nav-outline">Log In</a>
            <a href="/register" class="btn-nav-primary">Start Free</a>
        </div>
    </nav>

    <!-- HERO -->
    <section class="hero">
        <div class="hero-glow"></div>
        <div class="hero-grid">
            <div>
                <div class="hero-eyebrow">
                    <span class="eyebrow-dot"></span>
                    <span class="eyebrow-text">Now live — 400+ courses</span>
                </div>
                <h1 class="hero-title">
                    LEARN.<br>
                    BUILD.<br>
                    <span class="accent">GROW.</span>
                </h1>
                <p class="hero-subtitle">The LMS platform engineered for ambitious learners and world-class instructors.
                    Certify skills. Unlock opportunity.</p>
                <div class="hero-ctas">
                    <a href="/register" class="btn-primary">
                        Get Started Free
                        <i data-lucide="arrow-right" style="width:18px;height:18px;"></i>
                    </a>
                    <a href="/courses" class="btn-ghost">
                        <i data-lucide="play-circle" style="width:18px;height:18px;"></i>
                        Browse Courses
                    </a>
                </div>
                <div class="hero-social-proof">
                    <div class="avatars">
                        <span>👩</span><span>👨</span><span>🧑</span><span>👩</span><span>+</span>
                    </div>
                    <p class="proof-text"><strong>12,000+</strong> learners already enrolled</p>
                </div>
            </div>
            <div class="hero-visual">
                <div class="hero-card">
                    <div class="card-thumb">
                        <svg class="card-thumb-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
                            stroke="currentColor" stroke-width="1">
                            <polyline points="16 18 22 12 16 6" />
                            <polyline points="8 6 2 12 8 18" />
                        </svg>
                    </div>
                    <div class="card-body">
                        <p class="card-cat">Full-Stack Development</p>
                        <h3 class="card-title">Advanced React & Node.js Masterclass</h3>
                        <div class="card-progress-label">
                            <span>Course Progress</span>
                            <span style="color:#F5C518; font-weight:700;">68%</span>
                        </div>
                        <div class="progress-track">
                            <div class="progress-fill"></div>
                        </div>
                        <div class="card-footer">
                            <span class="card-rating">⭐ 4.9</span>
                            <span class="card-students">23,492 students</span>
                        </div>
                    </div>
                </div>
                <!-- Floating badges -->
                <div class="badge-float badge-float-1">
                    <div class="badge-icon"><i data-lucide="award" style="width:20px;height:20px;color:#F5C518;"></i>
                    </div>
                    <div>
                        <div class="badge-label">Certificate Earned</div>
                        <div class="badge-value">Full-Stack Dev</div>
                    </div>
                </div>
                <div class="badge-float badge-float-2">
                    <div class="badge-icon"><i data-lucide="trending-up"
                            style="width:20px;height:20px;color:#10B981;"></i></div>
                    <div>
                        <div class="badge-label">This Week</div>
                        <div class="badge-value">+847 Enrollments</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- STATS -->
    <div class="stats-bar">
        <div class="stats-inner">
            <div style="text-align:center;">
                <div class="stat-n">12K+</div>
                <div class="stat-l">Active Learners</div>
            </div>
            <div style="text-align:center;">
                <div class="stat-n">400+</div>
                <div class="stat-l">Premium Courses</div>
            </div>
            <div style="text-align:center;">
                <div class="stat-n">200+</div>
                <div class="stat-l">Expert Instructors</div>
            </div>
            <div style="text-align:center;">
                <div class="stat-n">95%</div>
                <div class="stat-l">Completion Rate</div>
            </div>
        </div>
    </div>

    <!-- HOW IT WORKS -->
    <section class="section">
        <p class="section-label">How It Works</p>
        <h2 class="section-title">Three Steps. Infinite Growth.</h2>
        <div class="steps-grid">
            <div class="step">
                <div class="step-num">01</div>
                <div class="step-icon-wrap"><i data-lucide="upload-cloud"></i></div>
                <h3>Create & Upload</h3>
                <p>Instructors drag-and-drop video lessons, quizzes, and resources. Go live in minutes, not weeks.</p>
            </div>
            <div class="step">
                <div class="step-num">02</div>
                <div class="step-icon-wrap"><i data-lucide="users"></i></div>
                <h3>Enroll Students</h3>
                <p>Learners browse, enroll, and pay securely. Real-time progress tracking keeps everyone accountable.
                </p>
            </div>
            <div class="step">
                <div class="step-num">03</div>
                <div class="step-icon-wrap"><i data-lucide="award"></i></div>
                <h3>Certify & Grow</h3>
                <p>Auto-issue verifiable certificates on completion. Track earnings and engagement in your analytics
                    dashboard.</p>
            </div>
        </div>
    </section>

    <!-- FEATURES -->
    <section class="features-section">
        <div class="features-inner">
            <div>
                <p
                    style="font-size:0.8rem; font-weight:700; color:var(--yellow); letter-spacing:3px; text-transform:uppercase; margin-bottom:0.75rem;">
                    Why Teachures</p>
                <h2
                    style="font-family:'DM Sans',sans-serif; font-size:2.5rem; font-weight:700; color:#fff; line-height:1.2; margin-bottom:2.5rem;">
                    Everything You Need to Teach at Scale</h2>
                <div class="features-list">
                    <div class="feature-item">
                        <div class="feature-icon-wrap"><i data-lucide="video"></i></div>
                        <div>
                            <div class="feature-title">HD Video with CDN Delivery</div>
                            <div class="feature-desc">Upload once, stream anywhere. Powered by AWS S3 + CloudFront for
                                zero-lag delivery.</div>
                        </div>
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon-wrap"><i data-lucide="clipboard-list"></i></div>
                        <div>
                            <div class="feature-title">Quiz Engine & Grading</div>
                            <div class="feature-desc">Auto-graded quizzes with instant feedback. Set passing thresholds
                                and conditional progression.</div>
                        </div>
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon-wrap"><i data-lucide="trophy"></i></div>
                        <div>
                            <div class="feature-title">Gamification & Leaderboards</div>
                            <div class="feature-desc">Badges, points, and leaderboards drive engagement and keep
                                learners coming back.</div>
                        </div>
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon-wrap"><i data-lucide="bar-chart-2"></i></div>
                        <div>
                            <div class="feature-title">Deep Analytics</div>
                            <div class="feature-desc">Track completion rates, revenue, student progress, and drop-off
                                points — all in one dashboard.</div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="feature-showcase">
                <p
                    style="font-size:0.75rem; color:var(--muted); font-weight:700; letter-spacing:2px; text-transform:uppercase; margin-bottom:1.5rem;">
                    Trainer Analytics</p>
                <div style="display:flex; flex-direction:column; gap:1.25rem;">
                    <div>
                        <div style="display:flex; justify-content:space-between; font-size:0.85rem; margin-bottom:6px;">
                            <span style="color:rgba(255,255,255,0.6);">React Masterclass</span>
                            <span style="color:#F5C518; font-weight:700;">92%</span>
                        </div>
                        <div style="height:6px; background:rgba(255,255,255,0.06); border-radius:3px;">
                            <div
                                style="height:100%; width:92%; background:linear-gradient(90deg,#F5C518,#FFD700); border-radius:3px;">
                            </div>
                        </div>
                    </div>
                    <div>
                        <div style="display:flex; justify-content:space-between; font-size:0.85rem; margin-bottom:6px;">
                            <span style="color:rgba(255,255,255,0.6);">Python & ML</span>
                            <span style="color:#F5C518; font-weight:700;">78%</span>
                        </div>
                        <div style="height:6px; background:rgba(255,255,255,0.06); border-radius:3px;">
                            <div
                                style="height:100%; width:78%; background:linear-gradient(90deg,#F5C518,#FFD700); border-radius:3px;">
                            </div>
                        </div>
                    </div>
                    <div>
                        <div style="display:flex; justify-content:space-between; font-size:0.85rem; margin-bottom:6px;">
                            <span style="color:rgba(255,255,255,0.6);">UI/UX Fundamentals</span>
                            <span style="color:#F5C518; font-weight:700;">85%</span>
                        </div>
                        <div style="height:6px; background:rgba(255,255,255,0.06); border-radius:3px;">
                            <div
                                style="height:100%; width:85%; background:linear-gradient(90deg,#F5C518,#FFD700); border-radius:3px;">
                            </div>
                        </div>
                    </div>
                    <div
                        style="border-top:1px solid var(--border); padding-top:1.25rem; display:grid; grid-template-columns:1fr 1fr; gap:1rem;">
                        <div>
                            <div style="font-family:'Bebas Neue',sans-serif; font-size:2rem; color:#F5C518;">$4,820
                            </div>
                            <div
                                style="font-size:0.75rem; color:var(--muted); text-transform:uppercase; letter-spacing:1px;">
                                This month</div>
                        </div>
                        <div>
                            <div style="font-family:'Bebas Neue',sans-serif; font-size:2rem; color:#10B981;">+34%</div>
                            <div
                                style="font-size:0.75rem; color:var(--muted); text-transform:uppercase; letter-spacing:1px;">
                                vs last month</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA BANNER -->
    <div class="cta-banner">
        <div>
            <h2>Start Teaching. Start Earning.<br>Today.</h2>
            <p>Join 200+ instructors building sustainable income on Teachures.</p>
        </div>
        <a href="/register" class="btn-primary" style="white-space:nowrap;">
            Create Free Account
            <i data-lucide="arrow-right" style="width:18px;height:18px;"></i>
        </a>
    </div>

    <!-- FOOTER -->
    <footer>
        <div>
            <div class="footer-brand-name">TEACH<span>URES</span></div>
            <p class="footer-tagline">A world-class LMS platform for modern learners and ambitious instructors.</p>
        </div>
        <div class="footer-col">
            <h4>Platform</h4>
            <a href="/courses">Browse Courses</a>
            <a href="/pricing">Pricing</a>
            <a href="/register">Become an Instructor</a>
        </div>
        <div class="footer-col">
            <h4>Company</h4>
            <a href="/about">About</a>
            <a href="#">Blog</a>
            <a href="#">Careers</a>
        </div>
        <div class="footer-col">
            <h4>Legal</h4>
            <a href="#">Privacy Policy</a>
            <a href="#">Terms of Service</a>
            <a href="#">Cookie Policy</a>
        </div>
    </footer>
    <div class="footer-bottom">
        <span>© 2026 Teachures. All rights reserved.</span>
        <span>Made with ❤️ for learners worldwide</span>
    </div>

    <script>lucide.createIcons();</script>
</body>

</html>